@isTest
public with sharing class Bot_LockSlotControllerTest {
    @isTest
    static void lockAndCreateTest() {
        Test.startTest();
        Bot_LockSlotController aot = new Bot_LockSlotController();
        Test.stopTest();
    }
}