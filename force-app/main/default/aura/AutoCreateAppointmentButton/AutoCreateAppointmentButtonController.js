({
    onInit: function(cmp, event, helper) {
        helper.init(cmp);
    }, 
    createAppointment: function(cmp, event, helper){
        helper.createAppointment(cmp);
    },
    handleToast: function(cmp, event, helper){
        helper.handleToast(cmp, event);
    }
})