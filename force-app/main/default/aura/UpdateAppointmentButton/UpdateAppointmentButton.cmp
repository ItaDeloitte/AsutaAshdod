<aura:component extends="c:BaseComponent" implements="flexipage:availableForAllPageTypes,force:hasRecordId"
  access="global">
  <!-- attrs -->
  <aura:attribute name="recordId" type="String" default="" />
  <aura:attribute name="sobjectType" type="String" default="" />
  <aura:attribute name="isLoading" type="Boolean" default="false" />
  <aura:attribute name="appointment" type="Map" />
  <aura:attribute name="screenMode" type="String" default="" />
  <aura:attribute name="updateLabel" type="String" default="Update" />
  <!-- handlers -->
  <aura:handler name="init" value="{!this}" action="{!c.onInit}" />
  <aura:handler name="destroy" value="{!this}" action="{!c.onDestroy}" />
  <aura:handler event="force:refreshView" action="{!c.onInit}" />
  <!-- events -->
  <!-- methods -->

  <aura:if isTrue="{!v.appointment}">
    <lightning:overlayLibrary aura:id="overlayLib" />

    <lightning:button label="{!v.updateLabel}" variant="neutral" onclick="{!c.onUpdate}" disabled="{!v.isLoading}" />

  </aura:if>
</aura:component>